# Generated by XcodeGen from project.yml - regenerate with `xcodegen generate`
# rather than editing or committing this directly, so there's nothing to
# merge-conflict on.
*.xcodeproj/

# Xcode
xcuserdata/
DerivedData/
.build/
*.xcworkspace/xcuserdata/
*.swiftpm/

# Holds the real Supabase project URL/anon key and API base URL - never
# committed. Copy Secrets.swift.example to Secrets.swift and fill it in.
CoreSync/Config/Secrets.swift

.DS_Store
